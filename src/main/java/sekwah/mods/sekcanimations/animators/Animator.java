package sekwah.mods.sekcanimations.animators;

/**
 * The animators do not track the data but rather handle and update the tracked data.
 *
 * Make sure animation packets are sent with the entity ids or something to define the entitiy specifically
 * then cast and check the hash map to allow the necessary animator to update it.
 */
public class Animator {
}
